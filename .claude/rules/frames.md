---
paths:
  - "app/api/frames/**"
  - "app/image-ui/**"
---

# Farcaster Frames

- Frame responses are server-rendered OG images (components in `app/image-ui/`)
- Frame interactions come via POST with frame action payloads
- PRO variant gated by NFT pass ownership (logic in `app/pro/`)
